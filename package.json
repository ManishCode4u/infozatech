{
  "name": "infozatech-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "frontend-install": "cd frontend && npm install",
    "backend-install": "cd backend && npm install",
    "install-all": "npm run frontend-install && npm run backend-install"
  }
}
